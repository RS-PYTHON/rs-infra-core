apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dask-gateway-servicemonitor
  labels:
    release: prometheus-operator
spec:
  selector:
    matchLabels:
      app: dask-gateway
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: metrics
      interval: 30s