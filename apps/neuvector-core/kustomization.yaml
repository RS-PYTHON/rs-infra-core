commonLabels:
  app.kubernetes.io/instance: "{{ app_name }}"
namespace: neuvector

helmCharts:
- name: core
  repo: https://neuvector.github.io/neuvector-helm/
  releaseName: "{{ app_name }}"
  version: 2.7.3
  namespace: neuvector
  valuesFile: values.yaml

resources:
  - ingress.yaml


secretGenerator:
- name: enforcer
  literals:
  - CTRL_USERNAME=ZXhwb3J0ZXI=
  - CTRL_PASSWORD=RndSb3ZhWXV2UzFEbmU2OEZIaEE=